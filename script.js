const form=document.querySelector('#search'),result=document.querySelector('#result'),cards=[...document.querySelectorAll('.card')];form.addEventListener('submit',e=>{e.preventDefault();const q=document.querySelector('#where').value.trim().toLowerCase(),type=document.querySelector('#kind').value;let n=0;cards.forEach(c=>{const place=c.dataset.place,okPlace=!q||place.includes(q)||q.includes('himachal')&&place==='himachal',okType=type==='all'||c.dataset.type===type;c.style.display=okPlace&&okType?'block':'none';if(okPlace&&okType)n++});document.querySelector('#stays').scrollIntoView({behavior:'smooth'});result.textContent=n?`${n} matching place${n>1?'s':''} found.`:'No exact match yet — try Chamba or Himachal.'});document.querySelector('#partnerBtn').onclick=()=>alert('Partner onboarding is the next module.');
